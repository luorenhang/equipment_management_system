server:
  port: 8083

spring:
  application:
    name: equipment-inspection-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        namespace: public
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        namespace: public
        file-extension: yml
    sentinel:
      transport:
        dashboard: localhost:8088
        eager: true
  datasource:
    url: jdbc:mysql://localhost:3306/equipment_management_system?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: edu.ngd.order.equipment.inspection.entity

logging:
  level:
    edu.ngd.order.equipment.inspection.mapper: DEBUG
    org.springframework.jdbc.core: DEBUG